# 受控测试数据

`desktop-test.html` 是原生采集验收目标，不是 Proactive 应用UI。仅含合成内容，无网络请求、真实日历或文件写入。

样例覆盖：Space激活按钮、多行Enter换行、提交后输入框清空、日程先改后纠正、弹窗关闭、长正文滚动。点击相同空白处用于重复事件测试。

只有采集器和调试UI实际运行后，才能据此记录原生证据验证结果。当前文件存在不代表AX或截图捕获通过。
