# Understanding 调用链与时序

单条记录顶部固定展示识别结果，下方为截图、原始输入 Prompt、观测、AX 树四个 Tab。观测中左侧选择模型轮次或工具，右侧查看 Input、Output、诊断和 Token。AX 树默认折叠，截图优先展示实际送入模型的图片，缓存缺失时明确标注为本地预览。工具依靠 toolCallId 关联，旧记录缺失关联时独立展示，不补造历史。

模型轮次和工具开始、结束均保存状态；图片只保存引用，输入图仍从原图片页查看。追踪文件按动作串行、原子替换，避免并行工具写坏 JSON。界面沿用现有 shadcn Button/Tabs。

观测页采用 understanding.run → agent.turn → tool 的 Span 树，输入输出以字段显示，JSON 单独检查。turn 是包含工具执行的整轮耗时，不能当作纯模型延迟。工具开始时记录所属轮次，旧追踪只能按 toolCallId 关联；无法关联的工具挂在 run 下并注明，不伪造上下文组装阶段。主 Agent 投递是关联信息，不是理解执行的子 Span。

列表 Title 与 Detail 分成两行，轻量缓存携带耗时、总 Token 和费用字段。当前模型 API 没有返回金额，显示“费用未提供”，不假定免费或按模型名称猜价；账单费用或价格配置尚未接通。

动作在采集开始前同步占位，完成采集才转入理解。理解保持并发，主 Agent 不得越过仍采集或理解中的前序动作。失败或未选中的采集释放序位；30 秒未完成采集记为超时并放行。旧库已入队顺序不重写；失败重试属于后续补投，不会撤回已投递动作。投递节点保留序位、等待对象、批次和回执时间。

## 2026-09-13 诊断范围

当前可读记录包含 PixPin 两条 keyboard_window_not_found：按键到达时未找到该应用的 AX 焦点窗口或普通层窗口。不能直接断言权限缺失；浮层、窗口关闭和应用未暴露焦点都需要更完整原生证据。

Dock 一条 click_window_owner_ambiguous：WindowServer 命中 Dock 全屏层，AX 命中另一 PID。这是目标归属冲突，不是模型理解失败。另有两条 skipped_busy，是采集并发容量已满。

调查时理解缓存目录已不存在，动作队列为空，无法确认之前模型错误。历史失败不重试、不删除。窗口匹配策略仍需针对真实浮层验证，不能用补拍其他窗口掩盖失败。

后续新记录诊断：14 次模型失败中，13 次 finishReason 为 length、1 次请求超时。旧请求固定 1024 输出 Token，部分请求约 980 Token 用于推理后正文被截断。现已移除应用设置的输出 Token 上限，由服务端决定；供应商截断归类为 understanding_output_truncated，不再与格式错误混用。工具步数与请求超时保持独立策略，未因移除输出上限而取消。

输入会话聚合尚未实现。当前 Space 与 Shift+Enter 仍可能按已选触发策略进入理解；不能把本次侧边栏和预算修正宣称为输入过程去重。
